
import React from 'react';
import { Navigate } from 'react-router-dom';

// Este componente foi substituído pela funcionalidade de Periodização Inteligente (IA) 
// integrada ao AppContext. Redirecionando para a nova ferramenta.
export default function WorkoutGenerator() {
  return <Navigate to="/periodization" replace />;
}
